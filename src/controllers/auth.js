const AuthController = {}

AuthController.registration = async () => {
  // Send Login Email
}

AuthController.accept = async () => {
  // Accept Login Request
}

AuthController.verify = async () => {
  // Check verification status for login
}

export default AuthController
