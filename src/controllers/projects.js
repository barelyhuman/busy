const ProjectsController = {}

ProjectsController.create = async () => {
  // create project
  // add mapping for owner
}

ProjectsController.get = async () => {
  // get current user projects
}

ProjectsController.getById = async () => {
  // get one project with needed details
}

ProjectsController.getProjectMembers = async () => {
  // Single api to get only members
}

ProjectsController.inviteUser = async () => {
  // add into mappings
}

export default ProjectsController
